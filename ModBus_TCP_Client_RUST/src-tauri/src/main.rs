//! Главная точка входа приложения Modbus TCP Slave Simulator.
//!
//! Это минимальный файл main.rs, который запускает Tauri-приложение.
//! Вся основная логика находится в lib.rs и других модулях.

// Отключает дополнительное консольное окно на Windows в релизной сборке.
// НЕ УДАЛЯТЬ! Без этого атрибута при запуске .exe будет появляться чёрное окно консоли.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

/// Точка входа в приложение.
///
/// Вызывает функцию `run()` из библиотечного крейта, которая:
/// - Инициализирует логгер
/// - Создаёт хранилище данных для регистров и коилов
/// - Создаёт Modbus TCP сервер
/// - Запускает Tauri-приложение с Vue-фронтендом
fn main() {
    modbus_tcp_client_rust_lib::run()
}
